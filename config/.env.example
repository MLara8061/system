# === BASE DE DATOS (LOCAL/PDO) ===
# Usado por: config/db.php (conexión PDO moderna)
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=system
DB_CHARSET=utf8mb4

# === BASE DE DATOS (PRODUCCIÓN - LEGACY/MYSQLI) ===
# Usado por: config/config.php → config/db_connect.php (conexión mysqli)
# IMPORTANTE: En producción, SIEMPRE define estas credenciales
DB_HOST_PROD=localhost
DB_USER_PROD=tu_usuario_produccion
DB_PASS_PROD=tu_password_produccion
DB_NAME_PROD=tu_base_datos_produccion

# === URL BASE (OPCIONAL) ===
# Para desarrollo local, se detecta automáticamente
# BASE_URL=http://localhost/system
# Para producción, descomentar y configurar:
# BASE_URL=https://tudominio.com

# === NOTAS ===
# 1. Copia este archivo como ".env" en el mismo directorio
# 2. El archivo .env está en .gitignore y no se sube al repositorio
# 3. Cada desarrollador debe tener su propio archivo .env
